version: '3'
services:
  postgres:
    restart: always
    image: postgres:12.2
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
    volumes:
      - local-datavolume:/var/lib/postgresql/data
    ports:
      - '5432:5432'
volumes:
  local-datavolume:
