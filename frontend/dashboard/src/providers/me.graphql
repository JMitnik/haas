query me {
    me {
      id
      email
      firstName
      lastName
      phone
      globalPermissions
      userCustomers {
        # user {
        #   privateDialogues(workspaceId: $workspaceId) {
        #   assignedDialogueIds
        #   workspaceDialogues {
        #     id
        #     title
        #     slug
        #     description
        #   }
		    # }
        # }
        isActive
        customer {
          id
          name
          slug
        }
        role {
          name
          permissions
        }
      }
    }
  }