const errors: {[key: string] : string } = {
  'GraphQL error: dialogue:existing_slug': 'Dialogue slug already exists',
  'GraphQL error: customer:existing_slug': 'Customer slug already exists',
};

export default errors;
