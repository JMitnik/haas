query GetUserCustomerFromCustomer($id: ID!, $userId: String!) {
  customer(id: $id) {
    id
    userCustomer(userId: $userId) {
      user {
        id
        email
        phone
        firstName
        lastName
      }
      role {
        name
        id
      }
    }
  }
}
