query GetInteraction($sessionId: String!) {
  session(id: $sessionId) {
    id
    ...SessionFragment

    delivery {
      ...DeliveryFragment
      campaignVariant {
        id
        campaign {
          id
        }
      }
      events {
        ...DeliveryEventFragment
      }
    }
  }
}
