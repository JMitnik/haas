query GetWorkspaceUsersConnects($filter: PaginationWhereInput){
  usersConnection(filter:$filter)
   {
    userCustomers {
      user {
        id
        firstName
        lastName
        globalPermissions
      }
    }
  }
}