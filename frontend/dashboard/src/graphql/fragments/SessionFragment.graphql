fragment SessionFragment on Session {
  id
  createdAt
  score
  originUrl
  totalTimeInSec
  device
  dialogueId

  nodeEntries {
    ...NodeEntryFragment
  }

  delivery {
    ...DeliveryFragment
  }

  dialogue {
    id
    title
    slug
  }

  followUpAction {
    values {
      shortText
    }
  }
}
