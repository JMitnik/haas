fragment IssueFragment on IssueModel {
  id
  createdAt
  topic {
    id
    name
  }
  teamCount
  basicStats {
    average
    responseCount
    urgentCount
  }
  actionRequests {
    id
    createdAt
    session {
      id
      mainScore
    }
    dialogue {
      id
      title
    }
    status
    assignee {
      id
      email
    }
  }
}
