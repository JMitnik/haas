query GetUserTours($userId: String!) {
  user(userId: $userId) {
    tours {
      featureTours {
        ...UserTourFragment
      }
      releaseTour {
        ...UserTourFragment
      }
    }
  }
}
