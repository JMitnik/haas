query GetDelivery($deliveryId: String) {
  delivery(deliveryId: $deliveryId) {
    id
    campaignVariant {
      id
      dialogue {
        slug
      }
      workspace {
        slug
      }
    }
  }
}
