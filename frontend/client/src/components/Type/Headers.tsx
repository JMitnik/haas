import * as UI from '@haas/ui';
import styled from 'styled-components';

export const MainHeader = styled(UI.H2)`
  color: inherit;
`;
export const SubHeader = styled(UI.H4)`
  color: inherit;
`;
export const Text = styled(UI.Span)`
  color: inherit;
`;
