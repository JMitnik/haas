type NodeEntry {
    id: ID! @id
    sessionId: String!
    relatedNode: QuestionNode!
    edgeChild: Edge
    values: [NodeEntryValue]
    depth: Int
}

type NodeEntryValue {
    id: ID! @id
    textValue: String
    numberValue: Int
}
