type Question {
    id: ID! @id
    questionnaireId: ID!
    title: String!
    questionType(type: QuestionType): String!
    overrideLeafId: Int
    conditions: [QuestionCondition]
    options: [QuestionOption]
    children: [Question!]
}