type QQuestion {
    id: ID! @id
    title: String!
    branchVal: String
    questionType: String!
    overrideLeafId: Int
    leafNode: LeafNode
    conditions: [QuestionCondition]
    options: [QuestionOption]
    children: [QQuestion]
}