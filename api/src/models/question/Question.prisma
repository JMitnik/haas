type QQuestion {
    id: ID! @id
    # questionnaire: Questionnaire! @relation (link: INLINE) 
    title: String!
    branchVal: String
    questionType: String!
    overrideLeafId: Int
    conditions: [QuestionCondition]
    options: [QuestionOption]
    children: [QQuestion]
}