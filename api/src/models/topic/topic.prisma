type Topic {
    id: ID! @id
    title: String!
    description: String!
    publicTitle: String
    logoUrl: String
    language: String
    topicUrl: String
    questions: [QQuestion!]! @relation(link: TABLE)
}
