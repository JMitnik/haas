# import *, Query, Mutation from './generated/prisma.graphql'

type Query {
    newSessionId: String
}

input Session {
    entries: [EntryInput!]!
}

input EntriesInput {
    entries: [EntryInput!]!
}

input EntryInput {
    data: EntryInputData!
}

input EntryInputData {
    nodeId: String!
    edgeId: String
    textValue: String
    numberValue: Int
    depth: Int
}

type Mutation {
    uploadUserEntries(uploadEntriesInput: UploadEntriesInput): String
}
