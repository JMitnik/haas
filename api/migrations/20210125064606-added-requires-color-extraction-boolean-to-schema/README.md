# Migration `20210125064606-added-requires-color-extraction-boolean-to-schema`

This migration has been generated by Cold-A-Muse at 1/25/2021, 1:46:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."CreateWorkspaceJob" ADD COLUMN "requiresColorExtraction" boolean   NOT NULL DEFAULT true
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210125062507-add-lambda-booleans..20210125064606-added-requires-color-extraction-boolean-to-schema
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource postgresql {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 // binaryTargets = ["debian-openssl-1.1.x"]
 generator client {
@@ -553,19 +553,20 @@
   FAILED
 }
 model CreateWorkspaceJob {
-  id                 String             @id @default(cuid())
-  createdAt          DateTime?          @default(now())
-  updatedAt          DateTime?          @updatedAt
-  name               String?
-  referenceId        String?
-  referenceType      CloudReferenceType
-  status             JobStatusType      @default(PENDING)
-  resourcesUrl       String?
-  requiresRembg      Boolean            @default(true)
-  requiresScreenshot Boolean            @default(true)
-  Job                Job
+  id                      String             @id @default(cuid())
+  createdAt               DateTime?          @default(now())
+  updatedAt               DateTime?          @updatedAt
+  name                    String?
+  referenceId             String?
+  referenceType           CloudReferenceType
+  status                  JobStatusType      @default(PENDING)
+  resourcesUrl            String?
+  requiresRembg           Boolean            @default(true)
+  requiresScreenshot      Boolean            @default(true)
+  requiresColorExtraction Boolean            @default(true)
+  Job                     Job
 }
 // Campaigns
 enum CampaignVariantTypeEnum {
```


