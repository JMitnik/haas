# Migration `20210125062507-add-lambda-booleans`

This migration has been generated by Cold-A-Muse at 1/25/2021, 1:25:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."CreateWorkspaceJob" ADD COLUMN "requiresRembg" boolean   NOT NULL DEFAULT true,
ADD COLUMN "requiresScreenshot" boolean   NOT NULL DEFAULT true
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210115090822-added-extra-fields-to-create-workspace-job..20210125062507-add-lambda-booleans
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource postgresql {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 // binaryTargets = ["debian-openssl-1.1.x"]
 generator client {
@@ -95,11 +95,11 @@
   tags             Tag[]
   isOnline         Boolean        @default(value: false)
   isWithoutGenData Boolean        @default(value: false)
-  wasGeneratedWithGenData Boolean @default(value: false)
+  wasGeneratedWithGenData Boolean           @default(value: false)
+  CampaignVariant         CampaignVariant[]
   @@unique([slug, customerId])
-  CampaignVariant CampaignVariant[]
 }
 enum NodeType {
   GENERIC
@@ -553,17 +553,19 @@
   FAILED
 }
 model CreateWorkspaceJob {
-  id            String             @id @default(cuid())
-  createdAt     DateTime?          @default(now())
-  updatedAt     DateTime?          @updatedAt
-  name          String?
-  referenceId   String?
-  referenceType CloudReferenceType
-  status        JobStatusType      @default(PENDING)
-  resourcesUrl  String?
-  Job           Job
+  id                 String             @id @default(cuid())
+  createdAt          DateTime?          @default(now())
+  updatedAt          DateTime?          @updatedAt
+  name               String?
+  referenceId        String?
+  referenceType      CloudReferenceType
+  status             JobStatusType      @default(PENDING)
+  resourcesUrl       String?
+  requiresRembg      Boolean            @default(true)
+  requiresScreenshot Boolean            @default(true)
+  Job                Job
 }
 // Campaigns
 enum CampaignVariantTypeEnum {
@@ -650,6 +652,6 @@
   id         String                 @id @default(cuid())
   status     DeliveryStatusTypeEnum
   createdAt  DateTime               @default(now())
   deliveryId String
-  Delivery   Delivery              @relation(fields: [deliveryId], references: [id])
+  Delivery   Delivery               @relation(fields: [deliveryId], references: [id])
 }
```


